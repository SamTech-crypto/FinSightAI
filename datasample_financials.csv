import pandas as pd
import numpy as np

def generate_dynamic_data(periods: int = 120, seasonal_periods: int = 12) -> pd.DataFrame:
    """Generates a large synthetic financial dataset with seasonality, trend, and noise."""
    dates = pd.date_range(start='2015-01-01', periods=periods, freq='M')

    # Simulate yearly seasonality with variation
    seasonal = 100 * np.sin(np.linspace(0, 2 * np.pi, seasonal_periods))  
    seasonal_repeats = np.tile(seasonal, periods // seasonal_periods + 1)[:periods]

    # Add multi-phase trend (simulate changing growth rates)
    base = 1000
    linear_trend = np.linspace(0, 3000, periods)
    slow_growth = linear_trend * np.where(np.arange(periods) < 60, 0.5, 1.2)

    # Introduce random shocks/noise
    noise = np.random.normal(loc=0, scale=250, size=periods)

    # Combine into amount
    amounts = (base + slow_growth + seasonal_repeats + noise).round(2)

    df = pd.DataFrame({'Date': dates, 'Amount': amounts})
    df.to_csv('financial_data.csv', index=False)
    return df
